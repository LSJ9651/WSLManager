# WSL 发行版管理工具 - 设计大纲

> **版本**: v1.0  
> **目标**: 提供一个可移植、自包含、用户友好的 WSL 发行版图形化管理工具（基于命令行菜单）


## 一、项目定位与设计目标

### 1.1 核心定位
- **通用性**: 脚本解压到任意目录即可使用，无需修改代码，支持 Win10/Win11
- **自包含性**: 所有 WSL 相关文件（母版、实例、备份）统一存放在脚本根目录下，整个文件夹可随意移动
- **易用性**: 用户双击 `.bat` 文件即可进入交互式菜单，无需记忆任何命令
- **安全性**: 危险操作（删除）需二次确认，提供分级清理选项

### 1.2 设计哲学
> **“.tar 做母版（存储），.vhdx 做实例（运行）”**
> - 母版仓库存储压缩的 `.tar` 文件，节省磁盘空间
> - 运行实例使用 `wsl --import` 生成的 `ext4.vhdx` 文件，性能最优


## 二、技术选型与架构

### 2.1 技术栈
| 组件 | 选择 | 理由 |
| :--- | :--- | :--- |
| **主脚本语言** | PowerShell 5.1+ | Win10/Win11 原生支持，无需额外安装 |
| **启动器** | Batch (.bat) | 解决双击运行和权限提权问题 |
| **配置文件** | JSON | 结构清晰，便于读写和版本管理 |
| **WSL 交互** | `wsl.exe` 命令 | 官方管理接口，稳定可靠 |

### 2.2 文件架构
```
WSLManager\                          # 📁 用户自定义根目录（可任意放置）
│
├── WSLManager.bat                   # 🚀 启动器（用户双击入口）
├── WSLManager.psm1                  # ⚙️ 核心功能模块
│
├── Config\                          # ⚙️ 配置目录
│   └── config.json                  # 用户配置文件（路径、偏好等）
│
├── Repositories\                    # 📦 母版仓库（存储 .tar 压缩包）
│   └── <发行版名>\
│       └── base.tar                # 干净的黄金母版
│
├── Instances\                       # 🖥️ 运行实例（存储 .vhdx 文件）
│   └── <实例名>\
│       └── ext4.vhdx               # WSL 实际运行文件
│
├── Backups\                         # 💾 备份存档
│   └── <实例名>\
│       ├── full_20260802.tar
│       └── full_20260715.tar
│
└── Temp\                            # 🧹 临时工作区（脚本自动清理）
    └── (运行时产生的中间文件)
```


## 三、核心功能模块设计

### 3.1 功能全景图
| 编号 | 功能 | 描述 |
| :--- | :--- | :--- |
| 1 | **列出所有实例** | 显示已安装的 WSL 发行版列表及运行状态 |
| 2 | **新增发行版** | 从在线商店/本地母版/自定义 `.tar` 创建新实例 |
| 3 | **备份发行版** | 将运行中的实例导出为 `.tar` 压缩存档 |
| 4 | **还原发行版** | 从备份 `.tar` 文件恢复实例 |
| 5 | **删除发行版** | 分级删除（仅注销/删备份/删母版/全量清理） |
| 6 | **退出** | 退出程序 |

### 3.2 各功能详细逻辑

#### 🔹 功能 2：新增发行版（重点设计）
```
用户选择新增
    ├── 来源1: 从微软官方在线商店下载
    │   ├── 显示可安装列表 (wsl -l -o)
    │   ├── 用户选择发行版 → wsl --install -d <名称>
    │   ├── 自动导出为母版 → Repositories\<名称>\base.tar
    │   ├── 注销默认位置实例 → wsl --unregister <名称>
    │   └── 导入到 Instances\ 下 → 用户自定义实例名
    │
    ├── 来源2: 从本地母版仓库创建
    │   ├── 扫描 Repositories\ 下所有含 base.tar 的文件夹
    │   ├── 用户选择母版 → 输入新实例名
    │   └── wsl --import <实例名> Instances\<实例名>\ base.tar
    │
    └── 来源3: 从自定义 .tar 文件导入
        ├── 用户指定 .tar 文件路径
        ├── 复制到 Repositories\<发行版名>\base.tar（作为母版）
        └── 同来源2 → 创建新实例
```

#### 🔹 功能 3：备份发行版
```
用户选择备份
    ├── 列出所有已安装实例 (wsl -l -q)
    ├── 用户选择要备份的实例
    ├── 生成备份文件名: full_YYYYMMDD_HHMMSS.tar
    ├── 执行 wsl --export <实例名> Backups\<实例名>\<文件名>.tar
    └── 提示备份成功及文件大小
```

#### 🔹 功能 4：还原发行版
```
用户选择还原
    ├── 列出 Backups\ 下所有实例的备份文件（按时间倒序）
    ├── 用户选择要还原的备份文件
    ├── 用户选择还原方式
    │   ├── 覆盖原实例（先注销再导入）
    │   └── 创建新实例（指定新实例名）
    ├── 执行 wsl --import <实例名> Instances\<实例名>\ <备份文件>.tar
    └── 提示还原成功
```

#### 🔹 功能 5：删除发行版（分级清理）
```
用户选择删除
    ├── 列出所有已安装实例
    ├── 用户选择要删除的实例
    ├── 选择删除级别
    │   ├── 1. 仅注销 WSL 实例（保留备份和母版）
    │   ├── 2. 注销 + 删除所有备份文件
    │   ├── 3. 注销 + 删除备份 + 删除母版
    │   └── 4. 全量清理（删除所有相关文件夹）
    ├── 二次确认（输入实例全名）
    └── 执行清理并反馈结果
```


## 四、配置管理设计

### 4.1 配置文件 (`Config\config.json`)
```json
{
    "WSLRoot": "",                    // 留空则自动识别为脚本所在目录
    "DefaultWSLVersion": 2,           // 默认 WSL 版本（1 或 2）
    "AutoCleanTempDays": 3,           // 自动清理 Temp\ 中超过 N 天的文件
    "BackupRetentionCount": 5         // 每个实例保留的最新备份数量（超过则自动删除旧备份）
}
```

### 4.2 路径解析逻辑（核心）
- **优先使用** `config.json` 中用户指定的 `WSLRoot`
- **若未指定**：自动使用 `$PSScriptRoot`（脚本所在目录）
- **所有子路径**（`Repositories\`, `Instances\`, `Backups\`, `Temp\`）基于 `WSLRoot` 动态拼接


## 五、安全与容错机制

| 机制 | 实现方式 |
| :--- | :--- |
| **管理员权限** | `.bat` 启动时自动检测并请求提权（`net session` + `Start-Process -Verb RunAs`） |
| **执行策略绕过** | `.bat` 中使用 `-ExecutionPolicy Bypass` 参数启动 PowerShell |
| **二次确认** | 删除操作需输入发行版全名确认，防止误触 |
| **路径有效性校验** | 每次读取配置后，使用 `Test-Path` 检查关键目录是否存在 |
| **操作日志** | 记录所有操作（新增/备份/还原/删除）到 `Logs\` 目录，便于追溯 |
| **异常捕获** | 关键操作使用 `try/catch` 捕获并显示友好错误信息 |
| **自动清理** | 脚本启动时自动清理 `Temp\` 中超过 N 天的旧文件 |
| **空间预警** | 备份前检测磁盘剩余空间，若不足 5GB 则发出警告 |


## 六、用户交互设计

### 6.1 菜单结构
```text
========================================
    WSL 发行版管理工具 v1.0
========================================
  1. 列出所有已安装的发行版
  2. 新增发行版
  3. 备份发行版
  4. 还原发行版
  5. 删除发行版
  6. 配置管理（查看/修改路径设置）
  7. 退出
========================================
请输入您的选择 (1-7)：
```

### 6.2 交互原则
- **信息提示**：使用不同颜色区分信息（`Cyan`=提示、`Yellow`=警告、`Red`=错误、`Green`=成功）
- **分步引导**：复杂操作采用向导式交互，每一步都有清晰说明
- **默认值友好**：输入框提供合理默认值，直接回车即可使用
- **操作反馈**：每个操作完成后显示明确的结果（成功/失败/跳过）


## 七、兼容性设计

| 平台 | 注意事项 |
| :--- | :--- |
| **Windows 10** | 版本 2004（内部版本 19041）及以上才支持 WSL 2；低版本自动降级为 WSL 1 |
| **Windows 11** | 原生支持 WSL 2，无需特殊处理 |
| **PowerShell 版本** | 兼容 PowerShell 5.1（Win10/11 预装版本），不依赖 PowerShell 7+ 特性 |
| **语言编码** | 脚本开头强制设置 `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`，确保中文显示正常 |


## 八、开发与部署建议

### 8.1 开发阶段
1. 先实现核心功能（列出、新增、备份、还原、删除）的原子操作
2. 再封装为模块函数，编写菜单交互逻辑
3. 最后增加配置管理和安全机制

### 8.2 部署使用
- 用户只需将 `WSLManager\` 文件夹解压/复制到任意位置
- 双击 `WSLManager.bat` 即可运行（首次使用会自动创建目录结构和配置文件）
- 整个文件夹可压缩打包发给其他用户，无需任何配置

### 8.3 后续可扩展方向
- 支持 WSL 1 / WSL 2 版本切换
- 支持计划任务自动备份
- 支持增量备份（仅备份变更的文件）
- 支持云存储同步（OneDrive / 阿里云盘等）


## 九、附录：关键命令速查

| 操作 | 命令 |
| :--- | :--- |
| 列出在线发行版 | `wsl -l -o` |
| 列出已安装实例 | `wsl -l -v` |
| 安装新发行版 | `wsl --install -d <名称>` |
| 导出为 tar | `wsl --export <实例名> <路径>.tar` |
| 导入为实例 | `wsl --import <实例名> <安装路径> <tar路径> --version 2` |
| 注销实例 | `wsl --unregister <实例名>` |
| 启动实例 | `wsl -d <实例名>` |
| 停止实例 | `wsl -t <实例名>` |


以上就是整理好的完整设计大纲。它覆盖了我们讨论的所有核心内容：**统一根目录结构、.tar 作为母版、实例与备份分离、分级删除、多来源新增**等。这份大纲可以直接作为开发的指导文档，也可以用来向团队或用户说明工具的设计思路。
