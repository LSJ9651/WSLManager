# WSL 发行版管理工具 - 设计提示词

> 以下为完整的功能需求和设计规范，可直接用作AI编码提示词

## 一、项目描述

开发一个用于管理 WSL（Windows Subsystem for Linux）发行版的 PowerShell 工具，用户通过双击批处理文件启动，在命令行菜单界面中完成对 WSL 发行版的**新增、备份、还原、删除**等管理操作。

## 二、核心设计原则

1. **完全可移植**：整个文件夹可随意移动（任意盘符、任意路径），脚本自动适配所在位置，无需用户修改任何代码
2. **自包含**：所有 WSL 相关文件（母版、实例、备份、临时文件）统一存放在脚本根目录下
3. **开箱即用**：首次双击启动即自动创建全部所需目录结构和默认配置文件
4. **存储策略**：`.tar` 压缩包作为母版存储（节省空间），`ext4.vhdx` 作为运行实例（性能最优）

## 三、目录结构规范

脚本根目录（用户自定义位置，如 `D:\WSLManager\`）下包含：

```
WSLManager\
├── WSLManager.bat          # 用户双击启动入口
├── WSLManager.psm1         # 核心功能模块
├── Config\
│   └── config.json         # 用户配置文件
├── Repositories\           # 母版仓库（存储 .tar 压缩包）
│   └── <发行版名>\
│       └── base.tar
├── Instances\              # 运行实例（存储 .vhdx 文件）
│   └── <实例名>\
│       └── ext4.vhdx
├── Backups\                # 备份存档
│   └── <实例名>\
│       ├── full_YYYYMMDD_HHMMSS.tar
│       └── ...
└── Temp\                   # 临时工作区（脚本自动清理）
```

## 四、配置管理规范

### config.json 结构

```json
{
    "WSLRoot": "",               // 留空则自动识别为脚本所在目录
    "DefaultWSLVersion": 2,      // 默认 WSL 版本（1 或 2）
    "AutoCleanTempDays": 3,      // 自动清理超过 N 天的临时文件
    "BackupRetentionCount": 5    // 每个实例保留的最新备份数量
}
```

### 路径解析逻辑

1. 优先使用 `config.json` 中 `WSLRoot` 字段指定的路径
2. 若未指定或路径无效，自动使用脚本所在目录（`$PSScriptRoot`）
3. 所有子路径动态拼接：`Repositories\`、`Instances\`、`Backups\`、`Temp\`

## 五、功能需求明细

### 功能 1：列出所有已安装的发行版

- 执行 `wsl -l -v` 显示所有实例及其运行状态
- 解析输出并以表格形式清晰展示

### 功能 2：新增发行版

提供三种来源选项：

**来源一：从微软官方在线商店下载**

1. 执行 `wsl -l -o` 显示可安装的发行版列表
2. 用户选择要安装的发行版（输入名称）
3. 执行 `wsl --install -d <发行版名>` 安装到系统默认位置
4. 导出为母版：`wsl --export <发行版名> Repositories\<发行版名>\base.tar`
5. 注销默认实例：`wsl --unregister <发行版名>`
6. 提示用户输入新实例名称（默认使用发行版名）
7. 导入到统一目录：`wsl --import <实例名> Instances\<实例名>\ Repositories\<发行版名>\base.tar --version 2`
8. 提示创建成功

**来源二：从本地母版仓库创建**

1. 扫描 `Repositories\` 下所有包含 `base.tar` 的子文件夹
2. 列出可用母版供用户选择（编号选择）
3. 用户输入新实例名称（默认自动生成：`<母版名>_YYYYMMDD`）
4. 执行 `wsl --import <实例名> Instances\<实例名>\ Repositories\<母版名>\base.tar --version 2`
5. 提示创建成功

**来源三：从自定义 .tar 文件导入**

1. 用户输入 `.tar` 文件的完整路径
2. 校验文件是否存在，不存在则报错返回
3. 自动提取文件名（不含扩展名）作为发行版名称
4. 复制 `.tar` 文件到 `Repositories\<发行版名>\base.tar`（作为母版）
5. 后续流程同来源二

### 功能 3：备份发行版

1. 执行 `wsl -l -q` 获取所有已安装实例列表
2. 用户选择要备份的实例（编号选择）
3. 生成备份文件名：`full_YYYYMMDD_HHMMSS.tar`（精确到秒）
4. 确保 `Backups\<实例名>\` 目录存在（自动创建）
5. 执行 `wsl --export <实例名> Backups\<实例名>\<文件名>.tar`
6. 显示备份文件大小
7. 若 `BackupRetentionCount` 设置大于 0，自动删除超出数量的旧备份（保留最新的 N 个）

### 功能 4：还原发行版

1. 扫描 `Backups\` 下所有子文件夹，收集其中的 `.tar` 备份文件
2. 列出所有可用备份（格式：`<实例名> - <文件名> - <文件大小>`）
3. 用户选择要还原的备份文件（编号选择）
4. 询问还原方式：
   - 选项 A：覆盖原实例（先 `wsl --unregister <原实例名>` 再导入）
   - 选项 B：创建新实例（用户输入新实例名）
5. 执行 `wsl --import <实例名> Instances\<实例名>\ <备份文件路径>.tar --version 2`
6. 提示还原成功

### 功能 5：删除发行版

提供四级清理选项：

1. **仅注销 WSL 实例**：执行 `wsl --unregister <实例名>`，保留备份和母版
2. **注销 + 删除备份**：注销实例并删除 `Backups\<实例名>\` 整个文件夹
3. **注销 + 删除备份 + 删除母版**：额外删除 `Repositories\<发行版名>\` 整个文件夹
4. **全量清理**：删除所有相关文件夹（包括 `Instances\<实例名>\`）

**安全机制**：
- 首先列出所有已安装实例供用户选择
- 显示红字警告提示操作不可恢复
- 要求用户输入实例完整名称进行二次确认（与选择的实例名完全匹配才执行）
- 确认后才执行删除操作
- 每步删除显示执行结果

## 六、代码规范

### 启动器脚本（WSLManager.bat）

```batch
@echo off
:: 自动请求管理员权限
net session >nul 2>&1
if %errorLevel% neq 0 (
    echo 正在请求管理员权限...
    powershell -Command "Start-Process '%~f0' -Verb RunAs"
    exit
)
:: 启动主脚本
PowerShell -NoProfile -ExecutionPolicy Bypass -Command "Import-Module .\WSLManager.psm1 -Force; Show-Menu"
pause
```

### 模块结构（WSLManager.psm1）

```powershell
# ========== 1. 编码与初始化 ==========
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8

# ========== 2. 全局变量定义 ==========
$script:Config = $null
$script:RootPath = Split-Path -Parent $PSScriptRoot

# ========== 3. 配置管理函数 ==========
function Get-WSLConfig { ... }
function Initialize-WSLConfig { ... }

# ========== 4. 路径管理函数 ==========
function Get-WSLRoot { ... }
function Ensure-Directory { ... }

# ========== 5. 核心功能函数 ==========
function Show-Menu { ... }
function List-Instances { ... }
function New-WSLInstance { ... }
function Backup-WSLInstance { ... }
function Restore-WSLInstance { ... }
function Remove-WSLInstance { ... }

# ========== 6. 辅助工具函数 ==========
function Write-ColorOutput { ... }
function Get-DistroNameFromTar { ... }

# ========== 7. 模块导出 ==========
Export-ModuleMember -Function Show-Menu
```

### 关键代码要求

1. **目录自动创建**：所有写入操作前使用 `New-Item -Force` 确保目录存在
2. **错误处理**：关键操作使用 `try/catch` 捕获异常，显示友好错误信息
3. **颜色反馈**：提示用 `Cyan`、警告用 `Yellow`、错误用 `Red`、成功用 `Green`
4. **执行结果检查**：每次 `wsl` 命令执行后检查 `$LASTEXITCODE`，非 0 则提示失败

## 七、交互规范

### 主菜单界面

```
========================================
    WSL 发行版管理工具 v1.0
========================================
  1. 列出所有已安装的发行版
  2. 新增发行版
  3. 备份发行版
  4. 还原发行版
  5. 删除发行版
  6. 退出
========================================
请输入您的选择 (1-6)：
```

### 交互原则

- 所有输入项提供清晰的提示文字
- 编号选择使用数字输入
- 名称输入提供默认值（直接回车使用）
- 危险操作前显示醒目的红色警告
- 每步操作完成后显示明确的结果反馈
- 操作完成后自动暂停，等待用户按键返回菜单

## 八、兼容性要求

| 项目 | 要求 |
| :--- | :--- |
| **操作系统** | Windows 10（版本 2004+）/ Windows 11 |
| **PowerShell** | 兼容 PowerShell 5.1（不依赖 7+ 特性） |
| **编码** | 使用 UTF-8 编码，确保中文显示正常 |
| **WSL 版本** | 同时支持 WSL 1 和 WSL 2，默认使用 WSL 2 |

## 九、安全与容错

| 机制 | 实现方式 |
| :--- | :--- |
| 管理员权限 | `.bat` 启动时自动检测并请求提权 |
| 执行策略绕过 | 使用 `-ExecutionPolicy Bypass` 参数 |
| 二次确认 | 删除操作需输入发行版全名确认 |
| 路径有效性 | 读写前使用 `Test-Path` 校验 |
| 异常捕获 | 关键操作使用 `try/catch` |
| 自动清理 | 启动时清理 `Temp\` 中超过 N 天的文件 |
| 空间预警 | 备份前检测磁盘剩余空间（低于 5GB 时警告） |
| 操作日志 | 可选：记录操作到 `Logs\` 目录 |

## 十、交付物清单

1. `WSLManager.bat` - 启动器脚本
2. `WSLManager.psm1` - 核心功能模块
3. 空的目录骨架（可自动生成，无需预置）
4. 使用说明文档（README.md）

## 十一、文件编码与换行符强制规范：
1. .bat / .cmd 批处理文件：必须使用 CRLF（Windows 行尾）作为换行符，编码必须使用 UTF-8 with BOM。严禁使用 LF，否则会导致命令解析错误。
2. .psm1 / .ps1 PowerShell 脚本文件：换行符统一使用 LF（Unix 行尾）以保持跨平台兼容性，编码必须使用  UTF-8 with BOM，以避免脚本解析时产生不可见字符干扰。
3. 生成代码后，请务必以符合上述格式规范的方式提供代码内容或保存文件。
---

> **生成要求**：请根据以上设计提示词，生成完整的、可直接运行的 `WSLManager.psm1` 模块代码。所有功能需完整实现，代码需包含详细的注释说明。